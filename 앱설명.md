# 생기부 도우미

키워드나 관찰 메모만 넣으면 **생활기록부 문장을 만들어 주는** 교사용 데스크톱 앱입니다.
학생 정보가 외부로 나가지 않도록 문장 생성·변형·학습을 **모두 선생님 컴퓨터에서** 처리합니다.

- **완전 오프라인 AI** — 인터넷 없이 내 PC에서 문장 생성 (모델 최초 1회만 내려받음)
- **단일 exe 배포** — `.NET 설치 없이` `생기부도우미.exe` 하나로 실행
- **학생 정보 안전** — 생성·학습·형태소 분석이 전부 로컬. 외부 서버로 안 나감

---

## 이런 게 됩니다

- **두 가지 방식으로 문장 만들기**
  - *내 문장 변형(같은 의미)* — 내가 쓴 대표 문장의 **고유명사·숫자는 그대로 두고** 서술어·어순만 바꿔, 의미가 같은 서로 다른 문장 여러 개를 만듭니다. (반 전체 문장이 똑같아 보이는 것 방지)
  - *키워드로 새로 생성* — 키워드·관찰 메모로 **새 문장을 여러 개** 만듭니다.
- **여섯 개 영역** — 세부능력 및 특기사항(세특) · 행동특성 및 종합의견(행특) · 자율활동 · 동아리활동 · 봉사활동 · 진로활동
- **엑셀식 학급 시트** — 학급 탭을 여러 개 두고, 선택한 학생 행에 문장을 채웁니다.
  - 행·열 삽입/삭제, 열 이름 변경, **열 고정·숨김**, **찾기·바꾸기**(현재 시트/전체 학급)
  - **엑셀 가져오기/내보내기**(.xlsx) — 기존 명단을 그대로 불러와 새 시트로
  - 셀 내용 자동 줄바꿈·행 높이 자동 맞춤, 복사/붙여넣기
- **실시간 형태소 점검** — 입력을 형태소로 나눠 색으로 표시. 드래그해 **용어 등록**하면 전문용어·고유명사(예: 아이오딘화 칼륨)의 철자가 보존됩니다.
- **생기부 규정 위반 경고** — 공인어학시험·수상·논문·대학명·부모정보 등 **기재 불가 항목을 감지**해 알려줍니다.
- **맞춤법 검사**(선택) — 동의할 때만 네이버로 문장을 보내 교정. 기본은 오프라인.
- **수업 메모 도구**(함께 설치) — 작업표시줄에 상주하는 가벼운 메모 앱. 수업 중 관찰을 **Ctrl+Alt+M**으로 빠르게 적어 학급 명단에 누적합니다(모델 불필요).

## 쓸수록 똑똑해집니다 (내 문체 학습)

- 마음에 드는 문장을 **저장**하면 내 컴퓨터의 학습 DB에 쌓입니다.
- 다음 생성 때 비슷한 예시를 찾아 참고하고, 내가 자주 쓰는 표현·어투를 반영합니다.
- **이 학습은 선생님 컴퓨터 안에서만** 이뤄집니다 — 다른 사람에게 공유되지 않습니다.

## 처음 실행하면

1. `생기부도우미.exe`를 더블클릭합니다. (설치 불필요)
2. **학습 모드 → 모델 내려받기**로 AI 모델을 받습니다. (약 4.7GB, 최초 1회. 형태소 모델은 처음 문장 분석 때 자동으로 받습니다.)
3. 이후에는 **인터넷 없이** 바로 씁니다.

> Windows 처음 실행 시 파란 보안 경고가 보이면 **추가 정보 → 실행**을 누르세요.

## 화면 구성 (상단 탭)

- **✍ 생성 모드** — 영역 선택 → 입력 → 문장 생성 → 학급 시트에 채우기
- **📚 학습 모드** — 모델 선택·다운로드, 학습 백업/복원, 용어 사전 관리
- **💬 메모** — 수업 메모 도구 자동 실행 설정 + 팝업 단축키 지정
- **🗺 과정 안내** — 입력 → 분석 → 생성 → 검증 → 채움 과정 안내

## 개인정보 · 안전

- **오프라인(기본)**: 문장 생성·변형·학습·형태소 점검·규정 검사 — **학생 데이터는 PC를 벗어나지 않습니다.**
- **온라인(선택)**: ① 최초 1회 모델 내려받기 ② 맞춤법 검사(동의 시에만). 이 둘 외에는 아무 것도 전송하지 않습니다.
- 학습 데이터·학급 명단은 **선생님 컴퓨터에만** 저장됩니다.

## 권장 사양

- Windows 10/11
- RAM 8GB 이상 / GPU 불필요 (CPU로 동작)
- 저장 공간 약 6GB (앱 + 모델)
- 속도보다 **품질·프라이버시 우선** — 한 세트 생성에 수십 초~수 분이 걸릴 수 있습니다.

## 주의

생성 결과는 **초안**입니다. 생기부 기재요령·사실관계·맞춤법은 **선생님이 반드시 최종 검토·수정**해 주세요.
